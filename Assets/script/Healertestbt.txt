tree("Root")
    repeat
        sequence
            tree("Idle")
            tree("Approach")
            tree("IdentifyWound")

tree("Idle")
    sequence
        Idle
        fallback
            IsIdle
            Succeed

tree("Approach")
    sequence
        Approach
        fallback
            IsReachedPlayer
            Succeed

tree("IdentifyWound")
    sequence
        IdentifyWound
        fallback
            IsPokebotSeated
            Succeed

tree("FindIngredients")
    sequence
        FindIngredients
        fallback
            AreAllIngredientsFound
            Succeed

tree("CreatePotion")
    sequence
        CreatePotion
        fallback
            IsPotionCreated
            Succeed

tree("DeliverPotion")
    sequence
        DeliverPotion
        fallback
            PotionDelivered
            Succeed

tree("AdministerPotion")
    sequence
        AdministerPotion
        fallback
            IsPotionGiven
            Succeed

tree("Celebrate")
    sequence
        Celebrate
        Succeed