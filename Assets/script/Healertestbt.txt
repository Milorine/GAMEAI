tree("Root")
    repeat
        sequence
            tree("Idle")
            tree("Approach")
            tree("IdentifyWound")
            tree("FindIngredients")
            tree("CreatePotion")
            tree("DeliverPotion")
            tree("AdministerPotion")
            tree("Celebrate")

tree("Idle")
    sequence
        Idle
        fallback
            IsPokebotDetected
            Succeed

tree("Approach")
    sequence
        Approach
        fallback
            IsPokebotReached
            Succeed

tree("IdentifyWound")
    sequence
        IdentifyWound
        fallback
            IsWoundIdentified
            Succeed

tree("FindIngredients")
    sequence
        FindIngredients
        fallback
            AreAllIngredientsFound
            Succeed

tree("CreatePotion")
    sequence
        CreatePotion
        fallback
            IsPotionCreated
            Succeed

tree("DeliverPotion")
    sequence
        DeliverPotion
        fallback
            PotionDelivered
            Succeed

tree("AdministerPotion")
    sequence
        AdministerPotion
        fallback
            IsPotionGiven
            Succeed

tree("Celebrate")
    sequence
        Celebrate
        Succeed